'use strict';

const BaseAgent = require('./baseAgent');
const ResearchAgent = require('./researchAgent');
const OutlineAgent = require('./outlineAgent');
const WriterAgent = require('./writerAgent');
const EditorAgent = require('./editorAgent');
const SEOAgent = require('./seoAgent');
const ImageAgent = require('./imageAgent');
const LinkedInAgent = require('./linkedInAgent');

module.exports = {
  BaseAgent,
  ResearchAgent,
  OutlineAgent,
  WriterAgent,
  EditorAgent,
  SEOAgent,
  ImageAgent,
  LinkedInAgent,
};
